/**
 * SEO/GEO toolkit barrel. Import builders + the metadata helper from one place:
 *   import { buildMetadata, organizationSchema, graph } from "@/lib/seo";
 */
export * from "@/lib/seo/schema";
export * from "@/lib/seo/metadata";
export * from "@/lib/seo/validate";
